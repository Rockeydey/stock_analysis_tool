{
    "environment": {
        "inputs": {
            "name": "local",
            "version": "1.0",
            "updated_on": "2026-06-11"
        },
        "required": "True",
        "description": "Configuration for local environment"
    },
    "threshold_config": {
        "inputs": {
            "discount_threshold": 2
        },
        "required": "True",
        "description": "Discount threshold interpretation: 1.1 means 10% discount, 1.2 means 20% discount, 2 means 50% discount, 4 means 75% discount, 10 means 90% discount"
    },
    "all_stocks_spec_path": {
        "inputs": {
            "all_stocks_spec_path": "config/all_stocks_spec.json"
        },
        "required": "True",
        "description": "Path to the all stocks specification file"
    },
    "input_data_path": {
        "inputs": {
            "all_stocks_data_path": "data/input/all_stocks_list.csv"
        },
        "required": "True",
        "description": "Path to the input data file containing stock information"
    },
    "output_path": {
        "inputs": {
            "json_output_stock_spec": "data/output/spec.json"
        },
        "required": "True",
        "description": "Path to the output file where the results will be saved"
    },
    "database_path": {
        "inputs": {
            "database_path": "C:/Users/rocke/OneDrive/1. daily_deliverables/1. master_folder/investment_strategy/project_automation/stock_db/data/stocks.duckdb"
        },
        "required": "True",
        "description": "Path to the database file stock data stored in DuckDB format"
    }
}